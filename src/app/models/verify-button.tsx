"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { BadgeCheck } from "lucide-react";
export function VerifyButton(){const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);const router=useRouter();async function run(){setBusy(true);setMessage("Testing due candidates against their providers…");try{const response=await fetch("/api/settings/automation/run",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({type:"CANDIDATE_VERIFICATION"})});const body=await response.json();if(!response.ok)throw new Error(body.error?.message??"Verification failed");if(body.started===false)setMessage("Verification already running");else{const processed=body.summary?.processed??0;setMessage(`${processed} candidate${processed===1?"":"s"} tested`)}router.refresh()}catch(error){setMessage(error instanceof Error?error.message:"Verification failed")}finally{setBusy(false)}}return <div style={{display:"flex",alignItems:"center",gap:8}}><span style={{fontSize:10,color:"var(--muted)"}}>{message}</span><button className="button" type="button" onClick={run} disabled={busy}><BadgeCheck size={14}/>{busy?"Verifying…":"Run verification"}</button></div>}
