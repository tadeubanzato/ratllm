import { PageShell } from "@/components/page-shell"; import { StatusPill } from "@/components/status-pill"; import { duration, timeAgo } from "@/lib/utils"; import { getDeployments, getSmokeTests, withDemo } from "@/server/queries"; export const dynamic="force-dynamic"; export default async function BenchmarksPage(){const [deployments,tests]=await Promise.all([getDeployments(),withDemo(()=>getSmokeTests(250),()=>[])]); const passed=tests.filter(t=>t.status==="PASSED").length; const failed=tests.filter(t=>t.status==="FAILED").length; const latest=new Map<string,typeof tests[number]>(); for(const t of tests)if(t.deploymentId&&!latest.has(t.deploymentId))latest.set(t.deploymentId,t); return <PageShell title="Benchmarks" eyebrow="Live LiteLLM operational evaluation"><section className="system-strip"><div className="system-item"><div><small>INVENTORY</small><strong>{deployments.length} deployments</strong></div></div><div className="system-item"><div><small>CHECK COVERAGE</small><strong>{new Set(tests.map(t=>t.deploymentId).filter(Boolean)).size}/{deployments.length}</strong></div></div><div className="system-item"><div><small>PASSED</small><strong>{passed}</strong></div></div><div className="system-item"><div><small>FAILED</small><strong>{failed}</strong></div></div></section><section className="panel"><div className="panel-header"><h3>Operational benchmark results</h3><span>Automatic daily chat-completion probe · newest result per deployment</span></div><table className="data-table"><thead><tr><th>Model / alias</th><th>Provider</th><th>Result</th><th>Latency</th><th>HTTP</th><th>Runs</th><th>Last tested</th></tr></thead><tbody>{deployments.map(d=>{const t=latest.get(d.id);return <tr key={d.id}><td><strong>{d.modelName}</strong><br/><span className="mono">{d.litellmModelName}</span></td><td>{d.providerName}</td><td><StatusPill value={t?.status??"NOT RUN"}/>{t?.error&&<><br/><span style={{fontSize:10,color:"var(--muted)"}}>{t.error.slice(0,100)}</span></>}</td><td className="mono">{t?.latencyMs?duration(t.latencyMs):"—"}</td><td className="mono">{t?.httpStatus||"—"}</td><td className="mono">{d.benchmarkRunCount}</td><td>{t?timeAgo(t.createdAt):"Never"}</td></tr>})}</tbody></table></section></PageShell>}