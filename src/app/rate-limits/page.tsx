import { PageShell } from "@/components/page-shell";import { getDeployments } from "@/server/queries";export const dynamic="force-dynamic";export default async function RateLimits(){const rows=await getDeployments();return <PageShell title="Rate Limits" eyebrow="Published, observed, safe, and manual limits"><section className="panel"><div className="table-scroll"><table className="data-table"><thead><tr><th>Deployment</th><th>Published</th><th>Observed</th><th>Safe</th><th>Manual override</th><th>Confidence</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td>{row.litellmModelName}</td><td>{row.rpmLimit??"—"}</td><td>—</td><td>{row.safeRpm??"—"}</td><td>Managed through API</td><td>{row.confidence}</td></tr>)}</tbody></table></div></section></PageShell>}