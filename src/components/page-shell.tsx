import { Topbar } from "./topbar";
export function PageShell({ title, eyebrow, children, actions, showSearch = true }: { title: string; eyebrow?: string; children: React.ReactNode; actions?: React.ReactNode; showSearch?: boolean }) { return <><Topbar title={title} showSearch={showSearch}/><main className="page"><div className="page-heading"><div>{eyebrow && <p className="eyebrow">{eyebrow}</p>}<h2>{title}</h2></div>{actions}</div>{children}</main></>; }
