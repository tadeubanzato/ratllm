# Provider adapters

Providers declare an adapter key and capability level: automated, partial, manual, or disabled. Credentials are references to server environment variable names.

Legacy Python adapters are behavioral references. They will be ported incrementally to a TypeScript contract supporting discovery, normalization, connectivity, published limits, and provider-specific normalized headers without fabricating unsupported APIs.
