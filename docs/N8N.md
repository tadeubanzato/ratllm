# n8n

n8n is the workflow scheduler and orchestrator; decision logic belongs in Curator services. Existing files under `n8n/` document the prior pipeline and will be replaced by the six requested authenticated workflows when internal orchestration endpoints are implemented.

`INTERNAL_API_SECRET` is reserved for constant-time authentication of n8n calls and must be distinct from every provider credential.
